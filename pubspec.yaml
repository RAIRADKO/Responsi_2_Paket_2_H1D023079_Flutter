name: responsi_mobile
description: "Responsi 2 Mobile Paket 2 H1D023079"
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.6.0  # Tambahkan ini (cek versi terbaru)
  http: ^0.13.5
  shared_preferences: ^2.1.0
  intl: ^0.18.0
  fluttertoast: ^8.0.9
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true